# The-beyond

AI Content Factory — hackathon project.

Team branches:
- `feature/frontend` — Frontend & UX
- `feature/backend-ai` — Backend & AI
- `feature/visual-engine` — Image & Visual Engine

The `main` branch should contain only integrated, working code.
